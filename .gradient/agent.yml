agent_environment: staging
agent_name: eduscout
description: 'ADK  agent. Invoke: curl -X POST <url> -H "Authorization:
  Bearer $DIGITALOCEAN_API_TOKEN" -H "Content-Type: application/json" -d ''{"prompt":
  "Hello, tell me what are the advantages of IDEAL school"}'''
entrypoint_file: main.py
